BLOCKER: Task 6 (Global review) not started. The run claims success but a fresh adversarial reviewer hasn't tried to falsify it. Until that happens, "the goal was met" is just lead-and-sub-agent self-assessment.

HYPOTHESIS: The acceptance evidence is concrete and observable: merge commit on main, three .wasm files, E2E test exits 0, 32 RTF rows in results.md. Likeliest falsifications: (a) a model the test claims to verify has hidden brokenness (e.g. WAV produced but inaudible silence — but ≥10KB threshold is weak); (b) the merge missed something on the feature branch (uncommitted file? renamed crate? stale tada-pkg?); (c) results.md rows might have garbage data (RTF=0.00x or wildly out-of-range); (d) "no regression" claim could be false if Pocket TTS or KittenTTS was working differently before the merge in a way the E2E test doesn't catch.

PLANNED FIX: Spawn fresh sonnet agent with global-review prompt, criterion-blind. The reviewer reads only goal + acceptance evidence (not the criterion), then tries to falsify. On PASS → proceed to self-review. On FAIL → reviewer appends Discovery block with specific re-fix tasks; loop continues.
