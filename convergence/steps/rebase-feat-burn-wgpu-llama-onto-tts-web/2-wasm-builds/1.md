BLOCKER: Task 2 not started. The three WASM packages (tts-wasm, kitten-wasm, tada-wasm) have not been built on merged main. `crates/{tts,kitten,tada}-wasm/pkg/*.wasm` artifacts may be stale or missing. Need to confirm all three compile cleanly post-merge.

HYPOTHESIS: tts-wasm and kitten-wasm built clean before the merge (no changes to them on the feature branch). tada-wasm is the unknown — the feature branch was where it was developed, and the merge resolved web/index.html conflicts but didn't touch Rust source. Likeliest failure modes: (1) Cargo workspace deps drift between feat-branch dev environment and main's pinned git deps for candle/mimi-rs; (2) WASM-specific feature flags getting exercised for the first time post-merge; (3) wgpu/Burn crate version compatibility issues with WASM target.

PLANNED FIX: Single-shot task. Delegate to a sonnet sub-agent: run all three `wasm-pack build` invocations in sequence (smallest blast radius first: tts-wasm → kitten-wasm → tada-wasm). On failure, triage and fix; do not silently disable features. Verify artifacts via `ls crates/{tts,kitten,tada}-wasm/pkg/*.wasm`. Commit any source fixes with run-slug prefix. Sub-agent reports per-build status + any fixes applied + final artifact paths.

## OUTCOME

**tts-wasm**: pass — 50s — `crates/tts-wasm/pkg/tts_wasm_bg.wasm` (1.7M)
**kitten-wasm**: pass — 48s — `crates/kitten-wasm/pkg/kitten_wasm_bg.wasm` (2.8M)
**tada-wasm**: pass — 2m 22s — `crates/tada-wasm/pkg/tada_wasm_bg.wasm` (13M)

Fixes applied: none

Convergence criteria met: all three exit 0; three .wasm files listed.
