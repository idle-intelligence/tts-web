BLOCKER: Task 3 convergence criterion not yet met. Step 1 produced a working test for Pocket TTS + KittenTTS but TADA audio generation is blocked: headless Chromium's `navigator.gpu` exists but `requestAdapter()` returns null, causing the Burn/wgpu LLM to panic with "No WebGPU adapter found." The criterion explicitly requires all three audio assertions to pass — currently 2 of 3.

HYPOTHESIS: Headless Chromium CAN do WebGPU on macOS with the right flags. The default Playwright launch options don't enable it. Candidates: `--enable-unsafe-webgpu`, `--use-angle=metal` (macOS Metal backend), `--enable-features=Vulkan,WebGPU`. Some setups also need `--enable-webgpu-developer-features` and a non-sandboxed context. Less likely but possible: a software-rasterizer fallback like SwiftShader can satisfy `requestAdapter()` for testing purposes (lower perf, but it would unblock the assertion).

PLANNED FIX: Single-step exploration. Delegate to a sonnet sub-agent to: (1) try Playwright `chromium.launch({ headless: true, args: ['--enable-unsafe-webgpu', '--use-angle=metal', '--enable-features=Vulkan'] })` and confirm `navigator.gpu.requestAdapter()` returns non-null inside the page; (2) if Metal/Vulkan don't work, try `--enable-features=Vulkan,UseSkiaRenderer` + `--use-gl=swiftshader`; (3) once an adapter is available, re-run the TADA audio assertion in the existing test; (4) commit the test launch-args fix. If NO combination produces a working WebGPU adapter in headless Chromium on this macOS Metal-backed machine, return — lead will then either (a) split TADA audio into a separate native-only verification step, or (b) update the criterion to reflect "TADA DOM verified in headless; TADA audio verified natively in Task 1."

## OUTCOME

**Approach taken**: A (macOS Metal backend) — worked, but required two additional fixes beyond the launch flags.

**Three-stage diagnosis and fix**:

1. **WebGPU adapter** — Approach A flags (`--enable-unsafe-webgpu --use-angle=metal --enable-features=Vulkan --enable-webgpu-developer-features`) made `navigator.gpu.requestAdapter()` return non-null (`vendor: apple`, backend: `BrowserWebGpu`). Adapter probe returned `ok`.

2. **Cache API quota** — First run with working WebGPU hit `QuotaExceededError: Quota exceeded` on `cache.put()`. The headless Chromium Cache API default quota is ~865MB; the GGUF is 1.3GB. CDP `Storage.overrideQuotaForOrigin` didn't propagate to the worker context. `ctx.addInitScript()` patches the page context only, not dedicated workers. Confirmed via buffer allocation tests that the WebGPU `maxBufferSize=256MB` was NOT the issue.

3. **Worker script interception** — Fixed by intercepting `tada-worker.js` via `ctx.route('**/tada-worker.js', ...)` and prepending a Cache API no-op stub (IIFE) to the script body. Critical detail: must use `route.fulfill({ response, body: PATCH + body })` not `route.fulfill({ contentType: 'application/javascript', body: ... })` — overriding the Content-Type from `text/javascript` to `application/javascript` silently breaks module worker loading in Chromium.

**WebGPU probe verbatim**: `[tada] Speakers: 8, Styles: 7, Section visible: true, WebGPU: ok`

**Per-model results** (final run):
- Pocket TTS: PASS — 84524 bytes in 1.7s
- KittenTTS: PASS — 572844 bytes in 4.4s
- TADA: PASS — 155540 bytes in 26.7s

**Wall clock**: 33.7s

**Final output**:
```
PASS: pocket-tts, kitten, tada generated real audio ≥ 10KB
```
Exit 0.

**Commit**: `49d204b` — `[rebase-feat-burn-wgpu-llama-onto-tts-web] Enable WebGPU in headless Chromium for TADA E2E`

**Convergence criterion met**: All three audio assertions pass. Task 3 complete.
