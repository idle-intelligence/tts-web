BLOCKER: Task 3 not started. No Playwright E2E test exists at `scripts/test_demo_e2e.mjs`. The "no regression" claim for Pocket TTS and KittenTTS, plus the "TADA voice selector works end-to-end" claim, are unverified. Need a single test that exercises real audio generation for all three models in headless Chromium against the live dev server.

HYPOTHESIS: All three models should work post-merge — Pocket TTS and KittenTTS were untouched at the source level (the merge only resolved web/index.html UI conflicts), TADA passed sanity inference. Likeliest gotchas: (1) `web/serve.mjs` may not serve `voices/` (matrix subdir), breaking TADA voice loading; (2) WASM packages may load from wrong paths post-merge — `web/index.html` resolves them via `pkg/`, `kitten-pkg/`, `tada-pkg/` symlinks or copies; (3) Playwright must wait for non-trivial async load (model download + WASM init) before clicking Generate.

PLANNED FIX: Single-shot task. Delegate to a sonnet sub-agent: (1) probe `web/serve.mjs` — if `voices/` isn't served, add a static route; (2) probe what WASM-pkg paths the frontend expects (likely `web/pkg/`, `web/kitten-pkg/`, `web/tada-pkg/` or symlinks at repo root) and ensure those resolve to the just-built `crates/<pkg>/pkg/` artifacts; (3) write `scripts/test_demo_e2e.mjs` using `@playwright/test` or raw `playwright` — start serve.mjs as a child, load localhost:8081, switch to each model tab in turn, click Generate, capture the audio blob (likely via `page.waitForResponse` or by reading the `<audio>` element's `src` after async generation), assert byte length ≥ 10KB, tear down server cleanly; (4) run the test once and confirm exit 0; (5) commit with run-slug prefix.

## OUTCOME

**Frontend wiring probe**:
- `serve.mjs` serves: `/` → `web/index.html`; `/pkg/` → `crates/tts-wasm/pkg/`; `/kitten-pkg/` → `crates/kitten-wasm/pkg/`; `/tada-pkg/` → `crates/tada-wasm/pkg/`; `/voices/` → repo-root `voices/` (includes `voices/matrix/`); `/kitten-model.safetensors` and `/kitten-voices.safetensors` → `../hf/kitten-tts-nano-0.8/`; GGUF files (`*.gguf`, `/tokenizer.json`) → repo root
- `voices/` served: YES (already present, including `voices/matrix/` via path prefix)
- WASM pkg path mapping: serve.mjs already has explicit routes for all three packages; no symlinks needed

**Per-model results**:
- Pocket TTS: PASS, audio bytes 84524 (~82KB), time to generate 1.6s. Voice `alba.safetensors` intercepted from HuggingFace URL and served from local HF cache (`~/.cache/huggingface/hub/.../embeddings_v2/alba.safetensors`). Key fix: must set text in `#textInput` BEFORE clicking the voice button — otherwise `isDemoGen=true` and `onDone` returns early without showing the audio section.
- KittenTTS: PASS, audio bytes 572844 (~560KB), time to generate 4.5s. Key fix: must wait for phonemizer ready (loaded async from `esm.sh/phonemizer@1.2.1` after model ready) before clicking voice button; otherwise generation errors with "phonemizer not loaded yet".
- TADA: PARTIAL — DOM wiring correct (8 speaker buttons, 7 style buttons, voice section visible); audio generation BLOCKED by WebGPU unavailability. `navigator.gpu` object exists in headless Chromium but `requestAdapter()` returns null → WASM panics with "No WebGPU adapter found". TADA uses Burn/wgpu for the LLM backbone with no CPU fallback path in the WASM. This is a headless environment limitation, not a demo bug. To test TADA audio: open a real browser or run in a display environment with GPU access.

**Fixes applied**: none (no demo bugs found — only test-side timing issues with IsDemoGen and phonemizer load sequencing)

**Test runtime**: 8.7s wall clock

**Convergence criteria met**: `node scripts/test_demo_e2e.mjs` exits 0 with Pocket TTS PASS (84524 bytes) and KittenTTS PASS (572844 bytes). TADA partial (DOM OK, audio WebGPU-blocked). Per task spec, TADA DOM wiring is verified (≥4 speakers, ≥7 styles populated).
