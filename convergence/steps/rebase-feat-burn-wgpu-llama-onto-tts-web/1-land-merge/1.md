BLOCKER: Task 1 not started. `feat/burn-wgpu-llama` is not yet rebased onto current main; `cargo check -p tada-core --features metal` and the sanity inference (`tada_generate` on ex01_default → /tmp/sanity.wav ≥ 50KB) have not been run on a rebased tip; main has not received the merge commit.

HYPOTHESIS: The 20-commit divergence between feat/burn-wgpu-llama and main is dominated by Cargo dependency churn (1b19c26 git-deps switch, 40a2679/e3a2829 mimi-rs pins). The feature branch likely has local-path overrides that conflict. Resolution policy per Assumptions: take main's git deps unconditionally. Most other files on the feature branch are net-new (TADA voice selector UI, Burn/wgpu paths, voice matrix JSON), unlikely to conflict.

PLANNED FIX: Delegate to a sonnet sub-agent: (1) fetch state, checkout feat/burn-wgpu-llama, attempt rebase onto main; (2) resolve all Cargo.toml/Cargo.lock conflicts in favor of main; (3) for any other conflicts, apply project-conventions judgment (favor main's reorg moves, keep feature-branch additions intact); (4) on rebased tip, run `cargo check -p tada-core --features metal` and the sanity inference; (5) merge to main with `--no-ff`. If rebase becomes intractable (3+ files with non-mechanical conflicts), abort and use the documented fallback: merge feat branch directly into main with conflict resolution on the merge commit. Sub-agent reports final state: where the rebase landed, whether sanity passed, what was conflicted.

---

## OUTCOME

**Strategy**: Fallback merge (not clean rebase). Rebase was aborted after the first commit hit conflicts in web/index.html — 40 commits would each repotentially conflict in the same UI file. Used `git merge --no-ff feat/burn-wgpu-llama` on main instead, resolving conflicts on the single merge commit.

**Conflicts resolved**:

- `.gitignore`: feature branch dropped entries added by main (docs/pocket-tts/, kitten-claude-archive/, convergence symlinks). Took main's version (kept all entries).
- `web/index.html`: 9 conflict regions, all in JS/HTML. Root cause: feature branch added TADA UI where main added KittenTTS UI; the two branches diverged at the same function slots. Resolution strategy:
  - Added TADA radio button to model selector (both KittenTTS and TADA)
  - Added `tadaVoiceSection` alongside `kittenVoiceSection` (kept both)
  - Added `tadaControls` sliders (Noise/CFG/Steps) alongside kitten speed slider
  - Added `tadaProgress` div and CSS
  - Added `isTadaCached()` function
  - Added `makeTadaClient()` function (from feature's branch content, which git had merged into `makeKittenClient` incorrectly)
  - Fixed `makeKittenClient()` to use kitten config (took main's side)
  - Fixed `updateUIForModel()` to handle all 3 models
  - Fixed `switchModel()` to handle `tada` case
  - Fixed generate button handler to pass TADA opts

**Cargo.toml**: No conflict — feature branch Cargo.toml was already on main's git-dep version after the merge (git auto-resolved in favor of main, which is correct).

**`cargo check -p tada-core --features metal`**: EXIT 0 (13.4s on already-cached deps)

**Sanity inference**:
- Path: `/tmp/sanity.wav`
- Size: 186,262 bytes (186KB, well above 50KB threshold)
- Wall-clock: ~1m 28s total (1m 15s compile + 7.3s generation + 1.6s decode)
- RTF: 3.74x on Metal
- Note: model emitted NO_EOS warning (hit max tokens without EOS token) — known behavior for short texts, audio is still valid

**Final main HEAD**: `be63c07` — merge commit with two parents (`1290941` main, `3c202db` feature HEAD)

**git log**: `be63c07 [rebase-feat-burn-wgpu-llama-onto-tts-web] Merge feat/burn-wgpu-llama: TADA-1B (Burn/wgpu LLM, Q8_0 VV, voice matrix UI, CFG fix, sampling)`

**Convergence criteria met**:
- `git log --first-parent main -1 --oneline` shows merge commit ✓
- Two parents in `git log -1 --format=%P` ✓
- `cargo check -p tada-core --features metal` exits 0 ✓
- `/tmp/sanity.wav` exists and is 186KB ≥ 50KB ✓
