BLOCKER: Task 4 convergence criterion (≥30 RTF rows in `voice-matrix-sweep` section of `docs/tada/results.md`) not yet met. Step 4.1 produced 2 of 32 rows (amazement + ex01_default). 30 voices remain.

HYPOTHESIS: The script is idempotent — running it again with no args will skip the 2 already-completed voices and process the remaining 30. Per smoke-test extrapolation: ~10s per voice × 30 = ~5 min real, ~8-12 min including Cargo overhead. Risks: (a) any voice may transient-fail and need a retry; (b) NO_EOS warnings expected for short canonical text — those are not failures.

PLANNED FIX: Single iterate-step. Delegate to a sonnet sub-agent: (1) run `bash scripts/tada/benchmark_voices.sh` with no args (full sweep, idempotent); (2) verify resulting `docs/tada/results.md` has ≥30 voice-matrix-sweep rows with non-empty RTF; (3) commit results.md + lab-notebook.md updates + any /tmp/tada_bench WAVs that should be tracked (they shouldn't — gitignored — but verify count); (4) if any voices failed, the script logs reproducer commands; document in OUTCOME but don't retry inside this step (failures retry on next loop wake-up via idempotency). After this, Task 4 [x] if ≥30 succeeded.
