BLOCKER: Task 4 not started. No `scripts/tada/benchmark_voices.sh` exists. `docs/tada/results.md` has no rows for the 32-voice matrix sweep (6 base + 26 matrix). Convergence criterion requires ≥30 RTF rows.

HYPOTHESIS: This task has two stages — (a) write a robust idempotent shell script that iterates both `voices/*.safetensors` and `voices/matrix/*.safetensors`, and (b) execute it for all 32 voices. Doing both in one step would be ~1-2 hours of compute; cleaner to do (a) + a 2-voice smoke run in this step, then leave [ ] for the next step to do the full sweep. The TADA generation pattern is established (Task 1 sanity = 7.3s for short text on Metal). 32 × ~10s = ~5min minimum but in practice closer to 30-60min including model load amortization across runs.

PLANNED FIX: Single step of an iterate task. Delegate to a sonnet sub-agent: (1) read the existing `docs/tada/results.md` and `docs/tada/lab-notebook.md` formats so new rows match exactly; (2) write `scripts/tada/benchmark_voices.sh` — bash, idempotent (skip voice if existing row in results.md has non-empty RTF), iterates both voice dirs, captures wall-clock + audio duration → RTF, appends rows + experiment blocks; (3) smoke-test on exactly 2 voices (one base, one matrix) to verify format; (4) commit script + smoke results. Leave Task 4 [ ] for the next loop step to run the remaining 30 voices.
