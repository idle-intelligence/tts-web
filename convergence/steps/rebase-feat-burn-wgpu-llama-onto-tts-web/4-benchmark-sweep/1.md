BLOCKER: Task 4 not started. No `scripts/tada/benchmark_voices.sh` exists. `docs/tada/results.md` has no rows for the 32-voice matrix sweep (6 base + 26 matrix). Convergence criterion requires ≥30 RTF rows.

HYPOTHESIS: This task has two stages — (a) write a robust idempotent shell script that iterates both `voices/*.safetensors` and `voices/matrix/*.safetensors`, and (b) execute it for all 32 voices. Doing both in one step would be ~1-2 hours of compute; cleaner to do (a) + a 2-voice smoke run in this step, then leave [ ] for the next step to do the full sweep. The TADA generation pattern is established (Task 1 sanity = 7.3s for short text on Metal). 32 × ~10s = ~5min minimum but in practice closer to 30-60min including model load amortization across runs.

PLANNED FIX: Single step of an iterate task. Delegate to a sonnet sub-agent: (1) read the existing `docs/tada/results.md` and `docs/tada/lab-notebook.md` formats so new rows match exactly; (2) write `scripts/tada/benchmark_voices.sh` — bash, idempotent (skip voice if existing row in results.md has non-empty RTF), iterates both voice dirs, captures wall-clock + audio duration → RTF, appends rows + experiment blocks; (3) smoke-test on exactly 2 voices (one base, one matrix) to verify format; (4) commit script + smoke results. Leave Task 4 [ ] for the next loop step to run the remaining 30 voices.

---

OUTCOME (commit a655e79):

**Script**: `scripts/tada/benchmark_voices.sh` — 173 lines, executable (`chmod +x`).

**Idempotency**: Greps `docs/tada/results.md` for `voice-sweep-<name>`; checks awk field 12 (RTF column) for `[0-9]+\.[0-9]+x` pattern. If found, skips. Second smoke run confirmed: both voices correctly skipped.

**Smoke results** (2/32 voices, Var-C VV-Q8 E-Q4 on Metal):

| Voice | Load | Gen | Decode | Audio | RTF | WAV size |
|-------|------|-----|--------|-------|-----|----------|
| amazement (base) | 0.8s | 6.8s | 2.6s | 3.3s | 2.06x | 312,982 B |
| ex01_default (matrix) | 0.7s | 6.9s | 1.6s | 1.9s | 3.63x | 186,262 B |

Both WAVs ≥ 50KB. Both rows appended to `docs/tada/results.md` `voice-matrix-sweep` section. Both blocks appended to `docs/tada/lab-notebook.md`.

**Notes**: Both voices triggered `NO_EOS` sanity warning (model generated max frames without EOS). This is consistent with prior runs on short text — WAVs are valid audio. The warning is captured in the lab-notebook notes field.

**Time estimate for full 32-voice sweep**: ~10s per voice (gen+decode ~9.5s, load ~0.8s amortized = same per run since `cargo run` reloads binary each time). 30 remaining × ~10s = ~5 min. In practice ~8-12 min accounting for Cargo overhead on first run (binary already compiled: ~1s startup). Full sweep: `bash scripts/tada/benchmark_voices.sh` with no args.

**Task 4 status**: Still [ ] — convergence criterion (≥30 RTF rows in `voice-matrix-sweep`) requires the remaining 30 voices. Next loop step runs the full sweep.
