BLOCKER: Task 6 not started. No `wasm-final` row in results.md. No `## Perf/Quality Tradeoff` or `## Root cause and follow-up` section in lab-notebook.

HYPOTHESIS: Path (d) — diagnose-not-fix — is the right outcome based on data. CFG and tasks_max both refuted (Tasks 4, 5). 5b identified the actual root cause (dispatch fragmentation, 797 GPU dispatches/AR-step + 354 readback stalls, vs 0 stalls in voice-guided phase) — structural work in Burn/wgpu's ODE solver, NOT a config tweak. The `wasm-final` row should reflect the actual current production state (= wasm-baseline values, since no measured config beats it without quality regression). Tradeoff section + Root cause section make the diagnosis the run's deliverable.

PLANNED FIX: Lead does this directly (documentation synthesis from existing data, not code). (1) Append `wasm-final` row to results.md mirroring wasm-baseline numbers (current production = no measured improvement worth landing); (2) write `## Perf/Quality Tradeoff` section in lab-notebook with verbatim baseline RTF + final RTF + cfg10 + tmax-1024 alternative RTFs + Chosen rationale + Quality regression line; (3) write `## Root cause and follow-up` section naming the dispatch-fragmentation/readback-stall root cause from 5b's profiling, citing the per-step / per-dispatch evidence, listing concrete next-run goals (Burn ODE-solver fusion, single-flush-per-step, GPU-resident tensor reuse); (4) commit; (5) mark Task 6 [x].

## OUTCOME

Path (d) — Diagnose-not-fix — taken. Lead executed directly (documentation synthesis, no sub-agent needed).

**wasm-final row appended** to docs/tada/results.md mirroring wasm-baseline values (RTF 6.49x). Encoded model column: `Var-C simd=on cfg=1.6 tasks_max=512` (production default — no measured improvement justified landing).

**Perf/Quality Tradeoff section** written in lab-notebook with:
- Comparison table: 8 configs measured (baseline + cfg10/16 + 4× tmax + final)
- Verbatim baseline RTF: `6.49x`
- Verbatim final RTF: `6.49x`
- Chosen rationale: no lever crossed measurement noise; cfg=1.0's 3% win came with quality concern (audio 36% shorter)
- `Quality regression: none observed.` line ✓

**Root cause and follow-up section** written with:
- Bottleneck named: dispatch fragmentation + CPU↔GPU readback stalls in Burn/wgpu's AR ODE-solver path
- Profiling evidence: 797 dispatches/AR-step (4.69× voice-guided), 354 readback stalls/AR-step (vs 0 in voice phase), 4680 OrderingBarriers/AR-step (4× voice phase), no single dominant GPU op (longest 4.79ms)
- Why native is faster: Metal command buffer auto-fusion + no JS↔WASM↔WebGPU boundary cost
- 4 ranked hypotheses (H1 dispatch fusion, H2 readback elimination, H3 decoder GPU port, H4 V8 GC) with evidence + likelihood + test cost
- Recommended next-run scope: H1+H2 Burn ODE-solver refactor (likely upstream work)

**Convergence criterion (Task 6 path d)**: results.md has wasm-final row ✓. Lab-notebook has `Perf/Quality Tradeoff` section heading ✓ containing both verbatim RTF strings (`6.49x` matches both rows) ✓ + `Quality regression:` line ✓. Acceptance criterion 2 OR-branch satisfied.

**Commit**: this step's commit
