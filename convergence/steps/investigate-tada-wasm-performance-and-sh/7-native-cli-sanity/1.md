BLOCKER: Task 7 (native CLI sanity) hasn't been verified post-merge. The acceptance criterion requires `/tmp/post-perf-sanity.wav` to exist at ≥ 50KB after running `cargo run --example tada_generate -p tada-core --release --features metal --` with the canonical fox phrase against `voices/matrix/ex01_default.safetensors`. Independent of the WASM perf investigation; needs to pass at run-end regardless of WASM outcome.

HYPOTHESIS: Native CLI on Metal already passed sanity in the prior run (Task 1 of the rebase run produced `/tmp/sanity.wav` at 186KB, RTF 3.74x). Nothing about the perf investigation should have touched native CLI code paths. This is essentially a re-verification that head still works.

PIVOT NOTE: Task 2 is blocked on user click (3 loop iterations, /tmp/wasm-timings.jsonl empty). Doing Task 7 now while waiting is a documented "wall workaround" — does not violate convergence discipline since the criterion is independently verifiable.

PLANNED FIX: Lead executes the cargo command directly (single shot, no sub-agent needed for this 1-liner). On exit 0 + file ≥ 50KB → mark Task 7 [x]. If it fails — investigate, that's a real regression.

## OUTCOME

**Pivot rationale**: Task 2 awaiting user click (third loop iteration with empty /tmp/wasm-timings.jsonl). Task 7 is independent and small; doing it now makes loop time productive.

**Command** (with required `--model` and `--tokenizer` flags — original criterion was incomplete):
```
cargo run --example tada_generate -p tada-core --release --features metal -- \
  --model /Users/tc/Code/idle-intelligence/hf/tada-1b/tada-1b-C-vvq8-eq4.gguf \
  --tokenizer /Users/tc/Code/idle-intelligence/hf/Llama-3.2-1B/tokenizer.json \
  --voice voices/matrix/ex01_default.safetensors \
  --text "The quick brown fox jumps over the lazy dog." \
  --output /tmp/post-perf-sanity.wav
```

**Result**: exit 0; `/tmp/post-perf-sanity.wav` = 159,382 bytes (≥ 50KB ✓).
- Load: 5.6s, Generation: 9.3s, Decode: 1.8s, Audio: 1.7s
- RTF (gen only): 5.63x
- (Prior rebase-run sanity was 7.3s gen + 1.6s decode, RTF 3.74x — slightly slower this time, may be cold-cache or system load. Not a regression worth investigating now.)

**Side fix**: updated acceptance criterion (4) to include `--model` and `--tokenizer`. The original command would have panicked at `--model required` (rs:172).

**Convergence criterion met**: command exits 0 + WAV ≥ 50KB.
