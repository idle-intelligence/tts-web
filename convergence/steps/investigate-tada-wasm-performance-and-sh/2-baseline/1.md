BLOCKER: Task 2 (record baseline wasm RTF row) cannot proceed via headless Chromium without significant crash risk. Last session, running `scripts/test_demo_e2e.mjs` with the TADA portion enabled crashed the user's Mac (kernel-level instability — the agent itself died, not just the test). The headless config combines WebGPU + Cache API stub injection + the 1.3GB GGUF model load, and on this Mac that combination is unstable. Re-attempting headless without first establishing whether the crash was reproducible is unsafe — a kernel crash kills any in-progress agent work and we lose the loop.

HYPOTHESIS: The same Task 1 worker instrumentation now emits `[tada-timing] {...}` to the browser console regardless of headless or manual. A real-browser run on `http://localhost:8081` (which is running, HTTP 200) is the safer path — it doesn't require the Cache stub trickery (real browsers have proper Cache quota for the 1.3GB GGUF) and it doesn't combine all the headless-specific hacks that triggered the crash. The user is in-session and can click a voice in their browser in seconds.

PLANNED FIX: Skip headless entirely on this attempt — go straight to the documented "Crash fallback" path in Task 2's body. Print clear instructions to the user, schedule a longer wakeup (~30 min) to allow time, and on next iteration write the `wasm-baseline` row using user-supplied timing values. This is exactly what the task body authorizes ("do NOT retry headless ... await user-supplied values").
