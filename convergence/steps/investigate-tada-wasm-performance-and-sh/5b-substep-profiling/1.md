BLOCKER: Task 5b not started. We have only top-level totals (gen_ms 14.5s, decode_ms 6.4s) — not enough granularity to find the actual bottleneck. Required: per-step gen timing, per-frame decode timing, native CLI parity, fresh Chrome trace post-instrumentation, longest single GPU op identification, ranked hypothesis register.

HYPOTHESIS: This is a multi-iteration task. Reasonable first iteration: add per-step gen + per-frame decode instrumentation to web/tada-worker.js (or directly into tada-wasm crate if the loop is on the Rust side), read tada_generate.rs CLI to see what native already logs (it has detailed timing per CLAUDE.md examples), run one measurement to validate the new instrumentation. That gets us 2 of 5 outputs. Next iterations: fresh trace + analyze-trace.py + longest GPU op + hypothesis register.

PLANNED FIX: Delegate to a sonnet sub-agent for iteration 1: (1) read crates/tada-wasm/src/lib.rs and crates/tada-core/src/ to find the LLM step loop and decoder loop boundaries; (2) read crates/tada-core/examples/tada_generate.rs to see what native CLI logs (and produce native fox-phrase numbers if needed for parity); (3) add per-step gen_step_ms instrumentation — emit array via postMessage AND each step via console.timeStamp / performance.mark for trace alignment; (4) add per-frame decode timing similarly; (5) run scripts/measure_tada_cfg_ab.mjs (or new script) to capture instrumented run, parse the per-step / per-frame arrays from the timing output, dump as a histogram (mean, median, p95, count) into lab-notebook; (6) commit instrumentation + first profiling data. Leave Task 5b [ ] for next iteration to do trace capture + hypothesis register.
