BLOCKER: Task 5b convergence criterion not yet met. Iteration 1 delivered (a) per-step gen histogram, (b) per-frame decode summary, (c) WASM-vs-native ratios. Remaining: (d) longest single GPU op identified by name and duration, (e) ranked "Hypotheses for next run" subsection with ≥3 candidates each cited with profiling evidence.

HYPOTHESIS: With per-step instrumentation in place (commit 6a08c6c) and `performance.mark('tada-step-N')` markers emitted, a fresh Chrome trace will let `scripts/analyze-trace.py` align worker steps with GPU spans and identify the longest operation. The key question to answer: WHY do AR VV steps balloon to 681ms while voice-guided VV steps run at 73ms? Likely candidates: (1) different shader/kernel path in `solve_flow_matching_burn` AR branch, (2) larger tensor sizes in AR phase (no pre-computed voice prompts to short-circuit), (3) increased GPU dispatches per step in AR (unique to that branch), (4) memory transfer cost between WebGPU passes that's avoided in voice-guided phase. The trace will reveal which.

PLANNED FIX: Delegate to a sonnet sub-agent for iteration 2: (1) write a Playwright capture script that uses `page.tracing.start({ path, screenshots: false, categories: 'devtools.timeline,disabled-by-default-devtools.timeline,disabled-by-default-gpu.intel,disabled-by-default-v8.cpu_profiler,blink.user_timing' })` (or whatever Chromium accepts) to capture a TADA fox-phrase inference trace; saves to /tmp/wasm-perf-fresh.json; (2) run scripts/analyze-trace.py against the fresh trace; (3) identify the longest single GPU op span by name and duration — should be inside an AR VV step; (4) write the "Hypotheses for next run" subsection in lab-notebook with ≥3 candidates ranked by likelihood + cited with profiling evidence. Mark Task 5b [x] when criterion met (all 5 outputs present in lab-notebook's profiling section).

---

## ITERATION 2 OUTCOME — Task 5b [x] COMPLETE

### Trace capture
- Script: `scripts/measure_tada_trace.mjs` (CDP Tracing API)
- Path: `/tmp/wasm-perf-fresh.json` — 121.4 MB, 725,875 events
- Run stats: gen_ms=14293, decode_ms=7345, RTF=6.76x, step_count=59 (confirmed 3 tiers)

### analyze-trace.py result
- `scripts/analyze-trace.py` could NOT find TADA worker via the CDP trace (no `thread_name` events with 'DedicatedWorker' in the CDP format). Fell back to manual Python analysis using `blink.user_timing` markers + GPU thread events.

### (d) Longest single GPU op: GPUTask, 4.79ms at step 16 (T2/voice-guided phase)
- The longest observable GPU operation in the Chrome trace is a `GPUTask` event on CrGpuMain lasting 4.79ms, occurring during step 16 (T2/voice-guided phase).
- The `WebGPU` subspan max inside a T3/AR step is **1.72ms**.
- There is NO single dominant GPU operation. The bottleneck is dispatch fragmentation: 797 individual CommandBuffer flushes per AR step, each ~0.73ms. Total GPU time per AR step = 579ms.
- The single most expensive "operation" is the aggregate set of `flow_matching` ODE forward passes — each flush maps to one WGSL compute dispatch.

### Hypothesis register (4 hypotheses, ranked)
1. **H1 (High): Burn/wgpu issues 797 GPU dispatches/step in AR vs 170 in voice-guided (4.69x) due to unmerged ODE-solver compute passes** — evidence: GPUTask counts from trace.
2. **H2 (High): AR VV worker stalls 403ms/step waiting for GPU results (354 × 1-5ms stalls vs 0 stalls in T2)** — evidence: worker RunTask gap analysis showing 12,118 gaps/step in T3 vs 0 in T2.
3. **H3 (Medium): CFG dual-forward path doubles AR matmul count with no voice KV-cache reuse** — evidence: CFG A/B showed 3.3% total RTF gain; T3 should show larger per-step improvement when CFG=1.0.
4. **H4 (Low-medium): V8 GC during AR steps adds ~11ms/step overhead from ODE solver tensor allocations** — evidence: 49.4 GC events/step in T3 vs 23.3 in T2; first T3 step GC total = 11ms.

### Convergence criterion (5/5)
- (a) per-step gen histogram ✓ (iter 1)
- (b) per-frame decode summary ✓ (iter 1)
- (c) WASM vs native ✓ (iter 1)
- (d) longest single GPU op ✓ (iter 2 — GPUTask 4.79ms, root: dispatch fragmentation 797/step)
- (e) ranked hypothesis register ✓ (iter 2 — H1/H2/H3/H4 in lab-notebook)
