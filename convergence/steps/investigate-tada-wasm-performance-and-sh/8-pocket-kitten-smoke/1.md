BLOCKER: Task 8 (Pocket TTS + KittenTTS smoke) not run since the perf instrumentation landed (commits 0e1e206, 6a08c6c, f7862e0). Need to confirm the worker timing instrumentation + Burn ODE-solver step changes haven't broken the existing Pocket TTS or KittenTTS demo paths.

HYPOTHESIS: The perf work touched ONLY tada-worker.js (TADA) + tts-client.js (worker option plumbing) + serve.mjs (timing/audio endpoints). Pocket TTS and KittenTTS workers are separate files (worker.js for Pocket, kitten-worker.js for Kitten) — should be untouched. Pocket+Kitten paths likely still pass. TADA portion has been running headless many times this run without machine crashes, so the historical "crashed the machine" concern from the prior session may not reproduce on the current Mac state. Try the full test first; if TADA portion is unstable, add the documented SKIP_TADA escape hatch.

PLANNED FIX: Lead runs `node scripts/test_demo_e2e.mjs` directly. If exit 0 with all 3 PASS lines → mark [x] cleanly. If only Pocket+Kitten pass and TADA portion misbehaves → add `SKIP_TADA` env-var check + `// SKIPPED:` comment per Task 8's spec, re-run with SKIP_TADA=1, mark [x] when Pocket+Kitten pass.

## OUTCOME

Ran `node scripts/test_demo_e2e.mjs`. Exit 0. All three models PASS:
- Pocket TTS: 84,524 bytes in 1.8s
- KittenTTS: 198,444 bytes in 3.4s
- TADA: 155,540 bytes in 45.1s
- Wall clock: 51.0s

No regression. The perf instrumentation (worker timing, `/timings` endpoint, `/audio` endpoint) didn't break Pocket TTS or KittenTTS. TADA worked headless without crashing — same hardware that previously crashed has been stable across many headless TADA runs in this convergence run.

No need for SKIP_TADA escape hatch — TADA portion passes cleanly. Task 8's "if TADA crashes" branch was speculative; the actual TADA path is fine.

**Convergence criterion met (path a)**: `node scripts/test_demo_e2e.mjs` exits 0 with all three PASS lines.

**Note on TADA timing in this E2E run**: 45.1s wall is slower than prior wasm-baseline measurements (~28s in commit 49d204b; ~14s gen + 6s decode in `wasm-baseline` row). Difference is the per-step instrumentation overhead (worker now appends to step_ms_array and emits performance.mark per step) plus possibly different inference path. Not a regression worth chasing — the perf goal of this run is the diagnosis (Task 5b) and the path-(d) outcome (Task 6).
