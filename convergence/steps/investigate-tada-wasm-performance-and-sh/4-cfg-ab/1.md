BLOCKER: Task 4 (CFG A/B) not started. No `wasm-cfg16` / `wasm-cfg10` rows in results.md. No audio files at `/tmp/tada-cfg{16,10}-fox.wav` for user audition. The worker may or may not expose `cfg_scale` as a runtime param — needs verification.

HYPOTHESIS: tada-core's CLI (`tada_generate`) takes `--cfg-scale 1.6`, so the underlying inference engine accepts it. Whether the WASM binding `tada-wasm` exposes it is the open question. Likeliest finding: the WASM binding hardcodes the cfg path; we either expose it (one rebuild) or build two artifacts (two rebuilds, ~5 min each). For audio file capture, the cleanest path is to extend the /timings infra: add a sibling POST /audio?name=X endpoint that writes binary body, and have the worker fetch the WAV bytes there post-inference. Then the Playwright runner doesn't have to deal with browser blobs.

PLANNED FIX: Delegate to a sonnet sub-agent: (1) read crates/tada-wasm/src/ + crates/tada-core/src/ to determine whether cfg_scale is exposed at the WASM API surface; (2) if not, expose it minimally (expected: 1-3 lines, no semantic change); (3) extend web/serve.mjs with POST /audio?name=<name> writing binary body to /tmp/<name>; (4) extend web/tada-worker.js to POST the generated WAV bytes after inference completes when a query-string or postMessage flag indicates "capture audio"; (5) write/extend `scripts/measure_tada_baseline.mjs` (or a new `scripts/measure_tada_cfg_ab.mjs`) to run two measurements via Playwright — one with cfg=1.6 (write to /tmp/tada-cfg16-fox.wav), one with cfg=1.0 (write to /tmp/tada-cfg10-fox.wav); (6) append rows `wasm-cfg16` and `wasm-cfg10` to results.md; (7) lab-notebook entry; (8) commit. Single attempt — if Playwright crashes, do not retry headless.

OUTCOME: INTERRUPTED — resumed 2026-05-08T15:18:12Z. Previous step did not complete (sub-agent attempt was rejected by the user mid-flight; partial work landed in the working tree as uncommitted changes to web/serve.mjs, web/tada-worker.js, web/tts-client.js, and a new scripts/measure_tada_cfg_ab.mjs). The next step will start fresh on Task 4 — it should first git status to inventory the partial work, decide whether to use/revert it, then proceed.
