BLOCKER: Task 4 (CFG A/B) was completed by step 4.2's sub-agent before quota crash, but never committed. Working tree has: docs/tada/results.md (wasm-cfg16, wasm-cfg10 rows), docs/tada/lab-notebook.md (wasm-cfg-ab entry), and an orphan step-1 OUTCOME prepend in convergence/steps/.../1-instrument/1.md (the OUTCOME content that was always intended for Task 1 but never committed). Both audio files exist at /tmp/tada-cfg16-fox.wav (153KB) and /tmp/tada-cfg10-fox.wav (98KB).

HYPOTHESIS: All convergence criteria for Task 4 are met by the existing artifacts. Just needs a clean commit and a [x] flip. Lead can do this directly (no sub-agent — it's commits + close).

PLANNED FIX: (1) Verify criterion artifacts; (2) commit the orphan step-1 OUTCOME prepend as a separate commit (separate logical change); (3) commit results.md + lab-notebook.md as Task 4 measurements; (4) mark Task 4 [x] + commit; (5) flag the CFG hypothesis interpretation in the step OUTCOME — sub-agent wrote "CONFIRMED — 3.3% faster" but the data actually shows CFG is NOT the throughput bottleneck (the apparent wall-time improvement comes from cfg=1.0 producing shorter audio, not from faster per-token cost). Task 9 (analysis section) will revisit; this step just records the raw measurement and accurate verdict.

## OUTCOME

**Step 4.2's sub-agent completed Task 4's work before crashing on quota.** Inventory of artifacts confirmed in working tree:

- `docs/tada/results.md`: rows `wasm-cfg16` (gen 14.52s, decode 6.34s, audio 3.20s, **RTF 6.52x**) and `wasm-cfg10` (gen 8.70s, decode 4.16s, audio 2.04s, **RTF 6.30x**), correct IDs matching the row-ID convention.
- `docs/tada/lab-notebook.md`: `wasm-cfg-ab` entry with both runs documented + QUALITY: UNVERIFIED flag preserved.
- `/tmp/tada-cfg16-fox.wav` (153,632 B) and `/tmp/tada-cfg10-fox.wav` (97,952 B) — both ≥ 50KB ✓.
- Infrastructure landed in commit 2e363de (cfg_scale runtime, /audio endpoint, WAV capture, measurement script).
- Orphan: step-1 OUTCOME content was prepended to convergence/steps/.../1-instrument/1.md by some prior linter/agent action; legitimate content, just never committed. Folded into a side commit.

**CFG hypothesis verdict (LEAD ANALYSIS, supersedes the sub-agent's lab-notebook claim):**
The sub-agent wrote "CONFIRMED — CFG-off is 3.3% faster." This framing is misleading.

- RTF (per-second-of-audio cost): 6.52x → 6.30x = **3.3% improvement**, essentially noise.
- Wall-time (absolute): gen 14.52→8.70s = 40% reduction. BUT audio duration also dropped 3.20→2.04s (36% shorter).
- Per-second-of-audio cost is essentially unchanged. The wall-time win is because cfg=1.0 produced **less audio** (likely earlier EOS), not because it was generating audio faster.

**Verdict: CFG is REFUTED as the throughput bottleneck.** The 1100ms post-CFG GPU gap from the trace is NOT explained by 2× CFG dispatch overhead — it's something else. Tasks_max sweep (Task 5) is now the strongest remaining hypothesis.

**Quality concern to flag for user audition:** the cfg=1.0 audio is 36% shorter — possibly truncated or content-different from cfg=1.6. User should listen to both /tmp/tada-cfg{16,10}-fox.wav before any decision to land cfg=1.0 as production default.

**Convergence criteria met:**
- ≥ 2 rows with engine=wasm + IDs `wasm-cfg16` / `wasm-cfg10` ✓
- Both audio files present and ≥ 50KB ✓

**Commits:**
- 2e363de: infrastructure (already committed)
- (this step): step-1 orphan OUTCOME, Task 4 measurements, Task 4 [x]
