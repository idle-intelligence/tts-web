BLOCKER: Task 4 (CFG A/B) still [ ]. Step 4.1 was INTERRUPTED — the prior sub-agent got partway: web/serve.mjs and web/tada-worker.js and web/tts-client.js have uncommitted edits (likely the /audio endpoint + WAV capture + cfg_scale plumbing), and scripts/measure_tada_cfg_ab.mjs is a new untracked file (~187 lines per a TS diagnostic earlier). No measurements run, no rows in results.md, no audio files in /tmp/.

HYPOTHESIS: The partial work in the working tree is likely close to functional — the prior agent got far enough that lints fired on unused vars in measure_tada_cfg_ab.mjs (which means the file parses). Best path: inspect what's there, finish what's missing, run the two measurements. Reverting and starting from scratch wastes ~30 min of the prior agent's progress.

PLANNED FIX: Delegate to a sonnet sub-agent: (1) run `git status` and `git diff` to inventory the partial work; (2) read each touched file end-to-end to verify it does what step 4.1's plan intended (cfg_scale exposure, /audio endpoint with binary body, WAV capture in worker, two-measurement script with --cfg arg); (3) fix any bugs / address the lint warnings (writeFileSync unused, rtfStr unused, secStr unused — these may be vestigial); (4) run scripts/measure_tada_cfg_ab.mjs twice (cfg=1.6 → /tmp/tada-cfg16-fox.wav, cfg=1.0 → /tmp/tada-cfg10-fox.wav) — single attempt each, no headless retries on crash; (5) append wasm-cfg16 + wasm-cfg10 rows to docs/tada/results.md (literal IDs); (6) lab-notebook entry with CFG hypothesis verdict + QUALITY: UNVERIFIED flag; (7) commit. If the partial work is too broken to rescue (>15 min of bug-fixing), revert and start cleanly per the original step 4.1 plan.
