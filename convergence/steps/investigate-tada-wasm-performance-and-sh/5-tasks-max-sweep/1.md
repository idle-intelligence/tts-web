BLOCKER: Task 5 (tasks_max sweep) not started. No `wasm-tmax-*` rows in results.md. The sweep needs ≥ 3 distinct tasks_max values measured, with literal IDs `wasm-tmax-<N>` so the criterion grep matches.

HYPOTHESIS: With CFG ruled out as the throughput bottleneck (Task 4 showed 3.3% RTF delta — noise), the next strongest hypothesis is dispatch batching via `tasks_max`. The trace's 1100ms GPU gaps with tasks_max=512 are concentrated single submissions of ~1230 dispatches each. Lower tasks_max → smaller submissions, more frequent worker→GPU handoffs (could let the worker overlap CPU work with GPU work). Higher tasks_max → fewer submissions, less dispatch overhead but worker idle longer per gap. The pre-CFG experiment showed tasks_max=512 was much better than tasks_max=32, so the sweet spot is probably between 256 and 2048. Will test {256, 512, 1024, 2048} (the 512 row will overlap with wasm-baseline conceptually, but a fresh measurement under same session conditions is needed for clean comparison).

PLANNED FIX: Delegate to a sonnet sub-agent: (1) edit `crates/tada-wasm/src/lib.rs:714` (verified location from Task 1) to set tasks_max=N, rebuild via `wasm-pack build crates/tada-wasm --target web --release -- --features wasm --no-default-features` (~2-3 min), restart dev server (the WASM changed), run measurement via `scripts/measure_tada_cfg_ab.mjs --cfg 1.6` (or a dedicated sweep script — reuse if cleaner). Repeat for tasks_max ∈ {256, 1024, 2048}. The 512 measurement is already wasm-baseline conceptually, but produce a fresh `wasm-tmax-512` row for clean apples-to-apples. (2) Append 4 rows with literal IDs `wasm-tmax-{256,512,1024,2048}` to docs/tada/results.md. (3) After sweep, RESTORE source to tasks_max=512 (production default — Task 6 picks the final value). (4) Lab-notebook entry naming the minimum-RTF value. (5) Commit. Build failures tolerated; ≥ 3 distinct values must succeed.

---

OUTCOME (2026-05-08, commit 22784ae):

- Values successfully measured: 4/4 (no build failures)
- RTF results:
  - tasks_max=256: gen=15.76s, decode=6.46s, audio=3.20s, RTF=6.94x
  - tasks_max=512: gen=14.54s, decode=6.45s, audio=3.20s, RTF=6.56x
  - tasks_max=1024: gen=14.62s, decode=6.35s, audio=3.20s, RTF=6.55x
  - tasks_max=2048: gen=14.72s, decode=6.33s, audio=3.20s, RTF=6.58x
- Minimum RTF: 6.55x at tasks_max=1024
- Speedup vs production (tmax=512): 1.002x = 0.2% improvement — within noise
- Key finding: tasks_max 512–2048 are all within noise of each other (~6.55–6.58x). tasks_max=256 regresses by 5.8% (6.94x vs 6.56x), confirming 512 is already at the diminishing-returns knee. tasks_max is NOT a meaningful optimization lever beyond 512.
- Source restored to tasks_max=512 ✓
- Convergence criterion: `grep -E '^\| wasm-tmax-[0-9]+' docs/tada/results.md | awk -F'|' '{print $2}' | sort -u | wc -l` = 4 ≥ 3 ✓
- Commit: 22784ae
