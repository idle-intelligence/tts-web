BLOCKER: Task 5 sweep is COMPLETE (4 values measured, RTF flat 6.55–6.58x for {512,1024,2048}, 256 regresses) but the result invalidates the run's planned next steps. Original Phase 2 hypotheses (CFG, tasks_max) BOTH refuted. Phase 3 (land best config) is now meaningless since no config beats baseline. The user has explicitly flagged this and asked: "Do we have enough profiling?" Answer: NO. We have totals only — not enough to find the actual bottleneck.

HYPOTHESIS: The path forward is sub-step diagnostic profiling. Without per-step LLM timing, per-frame decode timing, and a fresh post-instrumentation Chrome trace, we can't say whether the bottleneck is dispatch overhead, dequantize cost, decoder loop, shader compilation, or something else. Adding a "diagnose-not-fix" path to Task 6 lets the run converge on rigorous diagnosis even if no fix lands in this run — which is honest given what the data has revealed.

PLANNED FIX: Lead-only changes (no sub-agent, just queue surgery + commit):
1. Mark Task 5 [x] (sweep done, hypothesis refuted is a valid result).
2. Insert a Discovery task 5b before Task 6 — "Sub-step profiling: where does WASM time go?" with concrete required outputs (per-step gen_ms, per-frame decode_ms, native CLI parity, fresh Chrome trace + analyze-trace.py, longest GPU op, ranked hypothesis register).
3. Reframe Task 6 to add a path (d): diagnose-not-fix. The run can converge on profiling-grade diagnosis when the actual fix is too big for this run.

## OUTCOME

**Task 5 closed [x]** in commit 22784ae. Sweep measured 4 values:
- 256 → RTF 6.94x (regression: -5.8%)
- 512 → RTF 6.56x (production default — within noise of others)
- 1024 → RTF 6.55x (winner by 0.2%, noise)
- 2048 → RTF 6.58x

Conclusion: tasks_max above 512 is flat. tasks_max=256 is worse. **NOT a meaningful optimization lever.** Combined with Task 4's CFG-refuted result (3.3% delta = noise), the original goal-text's two named hypotheses are both dead.

**Discovery task 5b inserted** in queue, before Task 6:
- Required outputs: per-step gen timing, per-frame decode timing, native CLI parity numbers, fresh Chrome trace post-instrumentation, longest GPU op identification, ranked hypothesis register with ≥3 candidates.
- Convergence criterion: lab-notebook contains "Sub-step profiling" section with all 5 outputs.

**Task 6 reframed**: added path (d) "Diagnose-not-fix" — if profiling identifies a root cause requiring out-of-scope work, the run converges on rigorous diagnosis (lab-notebook tradeoff section + root-cause section) rather than forcing a fix that doesn't fit. Acceptance criterion's tradeoff branch already covers this; no acceptance change needed.

**Convergence criteria for THIS step**: queue surgery committed, Task 5 [x], Task 5b present and well-specified.

**Commit**: this step's commit
