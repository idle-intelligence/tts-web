BLOCKER: Task 1 not started. `web/tada-worker.js` does not yet emit `gen_ms` / `decode_ms` / `audio_duration_ms` after inference. The `tasks_max` source location was assumed by mesh to be `crates/tada-wasm/src/lib.rs:714` but unverified. SIMD128 status in the production wasm build is unknown — the analyzer's hint that decode regressed from 5.4s (simd128 experiment) to 6.2s (current trace) suggests SIMD may be silently off, but no audit has been run.

HYPOTHESIS: The TADA worker likely already has `performance.now()` timing somewhere (debug logs?) but doesn't expose it as structured `postMessage` data. Reading the worker first will reveal what's there. The `tasks_max` constant is named clearly; grep for it across the tada-wasm crate. SIMD audit via `wasm-objdump -d crates/tada-wasm/pkg/tada_wasm_bg.wasm | grep -cE 'i32x4|f32x4|v128|simd'` is a one-liner.

PLANNED FIX: Delegate to a sonnet sub-agent: (1) read `web/tada-worker.js` to understand current structure; (2) add `performance.now()` brackets around the LLM forward and the decoder, capture audio_duration from the resulting buffer (samples / sample_rate), `postMessage` and `console.log` an object with the three keys after each inference; (3) grep `crates/tada-wasm/src/` for `tasks_max` to verify the source location; (4) run `wasm-objdump -d crates/tada-wasm/pkg/tada_wasm_bg.wasm | grep -cE 'i32x4|f32x4|v128|simd'` and record the count in lab-notebook with the verified `tasks_max` source location and current value; (5) commit with run-slug prefix.
